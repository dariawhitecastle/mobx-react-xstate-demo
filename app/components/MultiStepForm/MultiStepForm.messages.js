/*
 * MultiStepForm Messages
 *
 * This contains all the text for the MultiStepForm component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.MultiStepForm'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MultiStepForm component!',
  },
})
