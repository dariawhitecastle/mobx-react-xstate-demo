import React from 'react'
import { storiesOf } from '@storybook/react'

import TextPartialLink from '../TextPartialLink'

storiesOf('TextPartialLink', module).add('Default', () => <TextPartialLink />)
